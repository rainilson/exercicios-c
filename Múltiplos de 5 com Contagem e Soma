#include <stdio.h>

int main() {

 int numero = 1;
 int soma = 0;
 int quantidade = 0;
	while(numero <= 50){
	 if (numero % 5 == 0){
	 
	 printf("%d\n", numero);
	 quantidade += 1;
	 soma += numero;
}
	 numero +=1;	
	}	
	printf("total de numeros %d\n", quantidade);
	printf("soma dos numros %d\n", soma);
    return 0;
} 
